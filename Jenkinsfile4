pipeline {
    agent any
		tools {
			maven "maven"
			}
    stages {
        stage('test') {
            steps {
                sh "mvn test"
            }
        }
		stage('build') {
            steps {
                sh "mvn package"
            }
        }
		stage('deploy') {
            steps {
                deploy adapters: [tomcat9(credentialsId: 'tomcat', path: '', url: 'http://43.205.215.64:8080')], contextPath: '/app', war: '**/*.war'
            }
        }
    }
}
